name: Create Github Release

on:
  push:
    branches:
      - main

jobs:
  push_main:
    uses: Seann-Moser/GithubWorkflows/.github/workflows/semantic_release_push_main.yml@main
    secrets:
      GH_CI_PAT: ${{ secrets.GH_CI_PAT }}